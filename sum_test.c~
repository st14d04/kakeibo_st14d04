#include <stdio.h>
#include "sum.h"

int main(void)
{
	if(sum(payments(1)) == 1){
		printf("test done.\n");
	}else{
		printf("test failed...\n");
	}

	if(sum(payments(2)) == 3){
		printf("test done.\n");
	}else{
		printf("test failed...\n");
	}

	return 0;
}
